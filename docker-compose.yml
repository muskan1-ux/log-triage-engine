version: "3.8"

services:
  kafka:
    image: apache/kafka:3.8.0
    container_name: log-triage-kafka
    ports:
      - "9092:9092"
    environment:
      KAFKA_NODE_ID: 1
      KAFKA_PROCESS_ROLES: broker,controller
      KAFKA_LISTENERS: PLAINTEXT://:9092,CONTROLLER://:9093
      KAFKA_ADVERTISED_LISTENERS: PLAINTEXT://localhost:9092
      KAFKA_CONTROLLER_LISTENER_NAMES: CONTROLLER
      KAFKA_LISTENER_SECURITY_PROTOCOL_MAP: CONTROLLER:PLAINTEXT,PLAINTEXT:PLAINTEXT
      KAFKA_CONTROLLER_QUORUM_VOTERS: 1@localhost:9093
      KAFKA_OFFSETS_TOPIC_REPLICATION_FACTOR: 1
      KAFKA_AUTO_CREATE_TOPICS_ENABLE: "true"
      KAFKA_NUM_PARTITIONS: 6

  redis:
    image: redis:7-alpine
    container_name: log-triage-redis
    ports:
      - "6379:6379"

  postgres:
    image: postgres:16-alpine
    container_name: log-triage-postgres
    environment:
      POSTGRES_DB: logtriage
      POSTGRES_USER: logtriage
      POSTGRES_PASSWORD: logtriage
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

  app:
    build: .
    container_name: log-triage-app
    depends_on:
      - kafka
      - redis
      - postgres
    ports:
      - "8080:8080"
    environment:
      KAFKA_BOOTSTRAP: kafka:9092
      REDIS_HOST: redis
      DB_HOST: postgres
      OPENAI_API_KEY: ${OPENAI_API_KEY:-demo}

volumes:
  pgdata:
